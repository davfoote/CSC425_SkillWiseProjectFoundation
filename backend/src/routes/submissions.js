// TODO: Implement submissions routes
const express = require('express');
const router = express.Router();

router.post('/', (req, res) => res.json({ message: 'submit work placeholder' }));

module.exports = router;
