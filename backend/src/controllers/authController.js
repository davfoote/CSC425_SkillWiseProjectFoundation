// TODO: Implement authController
// Minimal export to satisfy imports during scaffolding
module.exports = {
  register: async (req, res) => {
    res.status(200).json({ message: 'register placeholder' });
  },
  login: async (req, res) => {
    res.status(200).json({ message: 'login placeholder' });
  },
};
