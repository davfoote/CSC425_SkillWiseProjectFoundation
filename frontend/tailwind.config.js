/**
 * Tailwind CSS configuration for the frontend app.
 * Adjust `content` globs if you add other template/file types.
 */
module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}', './public/index.html'],
  theme: {
    extend: {},
  },
  plugins: [],
};
